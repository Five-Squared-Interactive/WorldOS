{
  "name": "@worldos/plugin-ros2-bridge",
  "version": "1.0.0",
  "description": "WOS plugin that bridges ROS2 topics and services to MQTT via rosbridge WebSocket",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@worldos/plugin-sdk": "*",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.6.0",
    "@types/ws": "^8.5.10"
  },
  "files": [
    "dist",
    "wos-plugin.yaml"
  ]
}
