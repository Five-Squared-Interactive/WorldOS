{
  "name": "@worldos/plugin-world-manager",
  "version": "1.0.0",
  "description": "World Manager plugin for WorldOS - world metadata, entity CRUD, templates, terrain",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@worldos/plugin-sdk": "*",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "files": [
    "dist",
    "wos-plugin.yaml"
  ]
}
