{
  "name": "@worldos/plugin-messaging",
  "version": "0.1.0",
  "description": "Messaging plugin for WorldOS - channels, DMs, and message history",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/admin dist/admin",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@worldos/plugin-sdk": "file:../wos-plugin-sdk",
    "better-sqlite3": "^11.0.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  },
  "files": [
    "dist",
    "wos-plugin.yaml"
  ]
}
